import { Text } from "@react-navigation/elements";
import { SafeAreaView } from "react-native-safe-area-context";

export default function main() {
    return (
      <SafeAreaView>
        <Text>Hier sind gespeicherte Turniere</Text>
      </SafeAreaView>
    )
}
